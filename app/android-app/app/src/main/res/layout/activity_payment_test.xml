<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/ui_surface_lowest">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Payment Service Test"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/ui_on_surface"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Test the payment initiation endpoint directly from the app"
        android:textSize="14sp"
        android:textColor="@color/ui_on_surface_variant"
        android:layout_marginBottom="16dp" />

    <!-- QR Code ID Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        app:boxBackgroundColor="@color/ui_surface_variant"
        app:hintTextColor="@color/ui_on_surface_variant">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_qr_code_id"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="QR Code ID"
            android:inputType="number"
            android:text="1"
            android:textColor="@color/ui_on_surface" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Amount Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        app:boxBackgroundColor="@color/ui_surface_variant"
        app:hintTextColor="@color/ui_on_surface_variant">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_amount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Amount (KES)"
            android:inputType="numberDecimal"
            android:text="100"
            android:textColor="@color/ui_on_surface" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Test Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_initiate_payment"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Test Payment Initiation"
        android:layout_marginBottom="16dp"
        android:textAllCaps="true"
        android:letterSpacing="0.08"
        android:textColor="@color/ui_on_primary"
        android:background="@drawable/bg_dashboard_primary_button"/>

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_marginBottom="16dp" />

    <!-- Response Display -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Response:"
        android:textStyle="bold"
        android:textColor="@color/ui_on_surface"
        android:layout_marginBottom="8dp" />

    <ScrollView
        android:id="@+id/sv_response"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/tv_response"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/ui_on_surface_variant"
            android:padding="8dp"
            android:background="@drawable/border_gray"
            android:fontFamily="monospace" />
    </ScrollView>

</LinearLayout>

